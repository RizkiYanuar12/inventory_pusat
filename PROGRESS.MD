# PROGRESS — SS_Inventaris (baca file ini + PRD.MD di awal sesi baru)

> Update file ini di akhir setiap sesi eksekusi. PRD.MD = mau dibangun apa; file ini = sudah sampai mana.

## Keputusan terkunci (ringkas; detail di PRD.MD)
- 1 aplikasi 2 permukaan; Supabase 5 tabel (isolasi outlet via filter token di API).
- 1 link permanen `/pesan/<slug>-<token8>` (token = identitas + WAJIB login username+password; route `/terima/<token>` link-only DICABUT total 2026-09-20 — surat jalan hanya Tab Surat Jalan di balik sesi) + sesi opaque 24 jam di memori (bukan JWT; restart = logout ulang, kredensial tetap) + cookie per-link `sesi_outlet_<token>` (banyak link hidup berdampingan; mismatch = 401 TANPA hapus sesi) + scrypt + rate-limit 10/mnt + kolom `username_outlet` (pre-set gudang 1×, terkunci) + `password_outlet` (self-set outlet 1×, NULL = klaim-pertama via link) + teks lupa-password di form login (SOP: hubungi gudang → reset via Kelola Akun). Reset-link DICABUT 2026-09-15 (token permanen; ganti password cukup).
- Hybrid 2026-09-14 (revisi: tabel `gudang` id=1, UPDATE-only; skema aktual `password_gudang` + `created_at`): outlet self-set pertama-kali (NULL + link valid → buat 1×, lalu login; gudang set-ulang via Tab Lainnya hub). Gudang 1 kredensial bersama (2 orang) di tabel `gudang` (`id` selalu 1 | `password_gudang` scrypt | `created_at`; seed via SQL developer, app hanya UPDATE id=1, tanpa INSERT); gate penuh semua halaman + endpoint gudang (tahap 2 terserap) + kill-switch `GUDANG_GATE=off`; login sekali → semua tab; mendarat `/homepage`/tujuan.
- Slot Senin & Kamis, cutoff 15:00 WIB; `hitungSlot()` + self-check.
- Status: BARU → DISETUJUI / DISETUJUI SEBAGIAN / DITOLAK (terminal) → SIAP KIRIM → DIKIRIM → DITERIMA / DITERIMA SEBAGIAN.
- Approve per item (Penuhi / Tolak + keterangan manual); item ditolak hangus, tanpa backorder.
- Record pengiriman lahir OTOMATIS + atomik saat approve; TIDAK ada tombol buat-manual. Fitur susulan `/kirim` DICABUT 2026-09-14 (tak dipakai; arsip lama tetap di Lacak).
- `Tandai Dikirim` manual → surat muncul di Tab Surat Jalan outlet; `Batalkan` hanya pre-laporan + alasan wajib (tanpa link lama).
- Terima: ceklis per baris, 1 tombol `Kirim Laporan Terima`, gudang read-only (surat jalan).
- Katalog outlet TANPA stock. Items JSON (mesin) + Ringkasan (manusia) + Alasan (Pengiriman saja, per-item).
- Loket pesan < 15:00 WIB tiap hari (tutup → 403 pesan persis); multi-pesan gabung ke BARU sehati (ID sama, `digabung: true`; hanya BARU). Outlet tanpa tombol batal (SOP: hubungi gudang → gudang Tolak semua + keterangan). Fonnte DICABUT 2026-09-14; notif gudang = lonceng dalam-web (BARU/gabung/lapor + suara); tanpa share link surat jalan (Salin Link + Kirim WA surat jalan DICABUT 2026-09-20 — outlet cek Tab Surat Jalan).
- BottomNav 5 item (Home→`/homepage`, badge BARU skip-fetch saat logout). Hub 3 tab (Daftar/Lacak/Lainnya); Daftar = tab alur Baru/Siap Kirim/Dikirim/Diterima/Ditolak/Semua + FIFO antrean (Selesai dipecah 2026-09-18). Outlet 3 tab (Pesan Baru | Riwayat | Surat Jalan).
- Satuan kanonik huruf-kecil (2026-09-18): simpan/baca selalu `kanonikSatuan()` (alias `pc→pcs`, `g→gr`); beda kapital = "Tidak ada perubahan".
- Masuk/Keluar eceran-saja (2026-09-18, revisi F7): dropdown satuan dicabut total; backend tolak non-eceran; dus/pack hanya info teks di kartu Inventory. `parseKonversi` server hidup hanya untuk jalur pesanan.
- Surat jalan cetak (F8, format spreadsheet + kolom Checklist Outlet): judul ikut outlet, A4 landscape, harga moving-average × qty, tombol print manual, endpoint baca `GET /api/surat-jalan/:idKirim`.

## Status eksekusi (2026-09-08)
- [x] 2026-09-15 Login outlet (SELESAI + UAT live lolos) — username terpisah (`username_outlet`, pre-set gudang 1× via Tab Lainnya lalu terkunci permanen; min 3 + UNIQUE) + password self-set (`password_outlet` scrypt, NULL = klaim-pertama 1× via link lalu auto-masuk) + sesi opaque `sesi_outlet` 24 jam terikat token-link + `wajibOutlet` di GET/POST pesan + rate-limit 10/mnt per slug+IP + ganti-sendiri outlet + reset-password gudang + cabut total reset-link (endpoint + `tokenOutletBaru` + tombol Reset; Kelola Link → Kelola Akun). Kolom dibuat user. UAT: 401 tanpa sesi, 403 belum-aktif, 401 generik, buatPertama, 409 kunci-ganda, 429, 404 reset-link, GET sesi 200. File: `backend/lib/auth.js|data.js`, `backend/routes/pesan.js|gudang.js`, `client.js`, `PesanOutletPage.jsx`, `DaftarPengirimanPage.jsx`. Baris uji di-NULL-kan, DB bersih (9 outlet kosong).
- [x] 2026-09-14 Login gudang (SELESAI + uji masuk lolos oleh user) — scrypt + sesi opaque `sesi_gudang` 24 jam + rate-limit 10/mnt + `POST /api/gudang/masuk|keluar` + `GET /api/gudang/sesi` + `POST /api/gudang/password` (UPDATE id=1, selalu wajib sesi; toggle mata di form login + ganti) + gate penuh (tahap 2 terserap) + `GUDANG_GATE=off` + route `/gudang-masuk` (tanpa nav) + `RequireGudang` semua route gudang + tombol Keluar di homepage (outline-danger + ikon + confirm) + flag sesi `sessionStorage('gudang-masuk')` (BottomNav skip-fetch saat logout, console bersih). File: `server.js`, `client.js`, `GudangMasukPage.jsx`, `RequireGudang.jsx`, `App.jsx`, `DaftarPengirimanPage.jsx`, `HomePage.jsx`, `BottomNav.jsx`, `migrasi_gudang.sql`. Forensik denied: skema aktual `password_gudang` + grant `service_role`. U38 lolos.
- [x] 2026-09-14 F8 surat jalan cetak (kode SELESAI; UAT lembar oleh user) — `GET /api/surat-jalan/:idKirim` (baca-saja, satuan+harga dari master, tanggal DD/MM/YYYY, barang-hilang tetap cetak) + route `/surat-jalan/:idKirim` + tombol Print Daftar+Lacak + CSS print A4 landscape. Live: tanpa sesi 401. Self-check 0 FAIL + build OK.
- [x] 2026-09-14 Cetak gate DIKIRIM + baris coret — SIAP KIRIM → tombol hilang + endpoint 409; line TOLAK tercetak urutan pesanan (nama+qty coret, harga/total `-`, total abaikan) + footnote. Self-check + build OK; isi UAT sesi asli oleh user.
- [x] 2026-09-14 Kirim parsial + note manual — `keputusan` terima `qtyKirim` (< pesan + ket wajib; stock vs kirim; parsial → SEBAGIAN; ringkasan `→ kirim`); `PutusForm` input qty + ket kondisional. Self-check + build OK; happy-path produksi UAT user.
- [x] 2026-09-14 Cabut route mati `/tambah-barang` — me-render komponen salah (`TransaksiBarangPage`, bug copy-paste; satu-satunya perujuk = `ScanPage` mati); fungsi tambah-baru sudah dicover `/input`. File dibiarkan di disk, import yatim dibuang. Build OK.
- [x] 2026-09-14 Cabut total scan (cabut keputusan lama "file scanner dilarang dihapus") — hapus `ScanPage` + `VerifikasiKirim` + `QrScanner` + `ConfirmCard` + `useScanner` + route `/scan` + `cekBarang` client + endpoint + dep `html5-qrcode`; `Form*`/`ManualVerifikasiKirim` tetap (dipakai `/input`). Self-check + build OK.
- [x] 2026-09-14 Cabut Fonnte (pengirimnya saja; cek stock + sampling acak + kartu homepage tetap) — hapus `kirimPesan()` + endpoint `/api/sendMessage` + ENV; 4 titik peristiwa + reminder + sampling jadi `console.log` (jejak sementara sampai lonceng jadi). Self-check OK.
- [x] 2026-09-14 Lonceng gudang + suara (F6) — tabel `notifikasi` + tulis di BARU/DIGABUNG/LAPORAN + 2 endpoint + `LoncengGudang` (badge/dropdown/baca; Home + hub) + bunyi naik-saja + getar + toggle. Self-check + build OK; SQL + U42 dengar-lihat oleh user.
- [x] 2026-09-14 Poll hub 20 dtk — interval Daftar+Lacak + guard anti-tumpuk + fetch saat visible + ganti tab; outlet tetap 30 dtk. Build OK.
- [x] 2026-09-14 Tombol Refresh + outlet — `RefreshButton` pil berlabel (spin keyframe sendiri, hover/tekan biru state-driven lalu disederhanakan jadi `outline-primary` kembaran Keluar); dipakai homepage + outlet (tombol manual di header outlet, fetch penuh). Build OK.
- [x] 2026-09-14 Route `/homepage` + redirect `/` (edit user + 1 baris redirect saya) — BottomNav Home → `/homepage`; link root lama tetap hidup. Build OK.
- [x] 2026-09-14 TransaksiTable anti-tabrakan — header kartu wrap + tanggal kembali `small` (11px terbukti kekecilan). Build OK.
- [x] 2026-09-14 FastMovingTable bar arus — `<td>` filler + lebar kaku dibuang; kolom kanan = bar proporsi Masuk/Keluar + total. Build OK.
- [x] 2026-09-14 Keranjang outlet di atas search — urutan keranjang → search statis → kategori sticky `top:64`. Build OK.
- [x] 2026-09-14 Redesign kartu Daftar hub — dinding-teks ringkasan diganti chip `✓ N dipenuhi`/`✕ M ditolak` + list penuhi ringkas + tolak diciutkan; tanggal ganda Batch≈Pesan disatukan. Build OK.
- [x] 2026-09-14 Tab alur hub ganti dropdown (FIFO) — Baru/Siap Kirim/Dikirim/Selesai/Semua + badge + search; antrean FIFO tertua-di-atas (via `dibuatPada` baru di API), arsip terbaru; default tab Baru. Self-check + build OK, live `dibuatPada` ada.
- [x] 2026-09-14 Rapi kartu Daftar — grup 1 baris tampil langsung (tanpa toggle), list penuhi ambang 4 + expander, meta tanggal 2 baris, tombol tutup di paling bawah list terbuka, toggle biru tegas. Build OK.
- [x] 2026-09-14 Tab Lainnya hub — Kelola Link + Password Gudang pindah dari Daftar (Daftar steril operasional). Build OK.
- [x] 2026-09-14 Gate penuh gudang (tahap 2 terserap): `wajibGudang` di semua endpoint gudang baca+tulis (jalur outlet tetap publik) + `sesi` bypass saat gate off + `RequireGudang` di semua route gudang + login mendarat `/homepage`/tujuan. Self-check + build OK.
- [x] 2026-09-15 Pecah `server.js` (1587 baris → 10 file, opsi A + `backend/`) — root `server.js` forwarder 2-baris (`node server.js` tetap jalan); `backend/server.js` setup + `lib/waktu|ringkas|konversi|auth|data` + `routes/barang|pengiriman|pesan|gudang`; `db.js` tetap di root; potong-pindah murni (rapi kecil: hoist `wajibGudang` + `mulaiServer/jalankanSelfCheck`). Verifikasi: `node --check` 10/10 + self-check identik 7/7+3/3 + route 22/22 + boot `:3100` Supabase OK (`/api/gudang/sesi` 401, `/api/pesan/xxx` 404). `server2.js` duplikat di-skip (diferensial via `PORT=3100`, tanpa file ganda); backup `server.lama.js` dihapus (git = jaring pengaman).
- [x] 2026-09-14 Loket 15:00 + gabung sehati + cabut Tab Buat (ganti P1b 1-aktif) — `pesanDibuka()` (<15:00 WIB tiap hari; tutup → 403 + kartu info pesan persis); multi-pesan gabung ke BARU sehati (se-ID dijumlah, `digabung: true`, lonceng DIGABUNG; hanya BARU); Tab Buat + `BuatForm` + F2 dicabut (hub = Daftar/Lacak/Lainnya). Self-check 10/10 + build OK.
- [x] 2026-09-14 Cabut total Kirim Susulan (F2b, tak dipakai) — tombol `+ Kirim` + route `/kirim` + file `BuatPengirimanPage.jsx` + endpoint `POST /api/pengiriman` + `buatPengiriman()` client dihapus; fungsi internal server tetap (dipakai approve). Arsip susulan lama tetap di Lacak; susulan baru kembali manual di luar sistem.
- [x] 2026-09-14 Forensik PSN-20260914-004: DITERIMA benar (paket hanya 1 item penuhi, 2 ditolak tak ikut paket; SEBAGIAN = kirim-vs-terima, bukan pesan-vs-terima).
- [~] 2026-09-14 Info "N dari M item" di surat jalan — DIBATALKAN atas permintaan user (terlalu banyak info); kode di-revert bersih (nol diff), U37/E37 dicabut.
- [x] Step 0.2 — `hitungSlot()`, `buatRingkasan()`, `formatTanggalSlot()`, `formatWaktuBukti()`, self-check 7/7 (`node server.js --self-check`). File: `server.js`.
- [x] Perbaikan umum — timestamp `weekday + timeZoneName WIB` tanpa detik (`server.js`); `dateParse` detik opsional (filter/sort aman); popup `ResultModal` di Tambah/Transaksi Barang; pagination `TransaksiPage`; SPA fallback Express 5 (`/{*splat}`).
- [x] T1 Kirim + Terima — backend: `buatIdKirim`, 6 endpoint pengiriman. Frontend: `client.js` +6 fn, `BottomNav` (+Pesanan, tanpa badge), `App.jsx` (route + nav kondisional), `DaftarPengirimanPage` (Daftar+Lacak), `BuatPengirimanPage`, `TerimaBarangPage`. Build OK.
- [x] 2026-09-09 Ringkasan/Alasan Pengiriman — `buatRingkasanKirim()` (`Pasta 10 -> 9`) + `buatAlasan()` (`Nama: ket; ...`) di `server.js`; kartu Daftar tampil Alasan bila ada. `buatRingkasan()` lama dipertahankan untuk Pesanan/T2. Self-check + build OK.
- [x] Step 0.1 (MANUAL user, 2026-09-09) — tab `Outlet` lama (2 baris, dibuang 2026-09-10).
- [x] 2026-09-10 Outlet → Supabase + hapus Sheet total — tabel `outlet` (`slug` PK | `token` UNIQUE | `outlet`); `server.js` baca/tulis Supabase (kontrak API nol berubah); `npm remove google-spreadsheet google-auth-library`, hapus `ss-inventory-bot-key.json` + `SPREADSHEET_ID`; 9 outlet CSV ganti 2 lama (token baru).
- [x] T2a backend pesan (2026-09-09, live-test lolos) — `GET /api/pesan/:token` (identitas + katalog tanpa stock + jadwal live + riwayat milik token + bolehPesan/pesananAktif), `POST /api/pesan/:token` (BARU + `hitungSlot` + guard 409 1-aktif + WA admin), `GET /api/pesanan`; `client.js` +3 fn. Baris uji dihapus, DB bersih.
- [x] T2c halaman outlet (2026-09-09, build OK + route live 200) — `PesanOutletPage.jsx` 2 tab (Pesan Baru: search + keranjang tanpa stock + nama wajib + kartu blokir saat aktif | Riwayat: badge aktif + filter Aktif/Selesai/Semua + badge warna PRD + alasan DITOLAK + salin link DIKIRIM), route `/pesan/:slugToken` (token = segmen setelah strip terakhir), tanpa BottomNav.
- [x] T2b keputusan (2026-09-09, e2e lolos + DB bersih) — `POST /api/pesanan/:id/keputusan` (guard BARU/409, set eksak, TOLAK wajib ket, semua-tolak → DITOLAK tanpa kirim/stock; else validasi stock → `buatPengiriman()` internal → DISETUJUI/SEBAGIAN), `POST /api/outlet/:slug/reset-link` (token 8 char, lama mati), mirror P2c best-effort di kirim/batal/terima; `client.js` +2 fn. Tanpa WA di titik ini.
- [x] T2d hub gudang (2026-09-09, build OK + live lolos + DB bersih) — `/pesanan` 3 tab: Daftar (Kelola Link Salin/Reset di atas + filter status/cari + PutusForm per item + Tandai/Batal via kiriman relasi + terminal read-only), Buat (dropdown outlet + keranjang + nama → endpoint outlet yang sama, ikut blokir), Lacak (+Alasan); badge BARU di BottomNav (refresh tiap navigasi); `GET /api/outlet` (gudang-internal, termasuk token — konsisten dgn /api/pengiriman); `client.js` +fetchOutlet. Deviasi plan: tanpa `POST /api/pesanan/manual` (token via GET sudah cukup, lebih sedikit endpoint).
- [x] T2e WA admin (2026-09-09, e2e lolos + DB bersih) — fire-and-forget di Tandai (`*LINK BERITA ACARA*` + path) + konfirmasi (`*LAPORAN TERIMA*` + ringkasan/alasan/penerima); bukti 3x `Fonnte response` di log. Tanpa WA di titik putus.
- [x] Fondasi Supabase (2026-09-09) — `db.js`; kontrak API + frontend NOL berubah; PK natural (transaksi identity `id_transaksi`), FK RESTRICT + index, `dibuat_pada` utk urutan; kurang-stock atomik. Pelajaran: kolom angka numeric (bukan text), tulis-harus-urut hormati FK. Skema `barang_inventory`/`transaksi` diganti spec CSV final (lihat T4).
- [x] 2026-09-10 Rename kolom gudang — `kategori→kategori_bahan`, `satuan_eceran→satuan`, `satuan_grosir→satuan_gudang` (konversi grosir dicabut), `jumlah_stock→total`, `batas_restock→minimum_stock`, `id_transaksi` identity (hapus `nextIdTransaksi` dari tulis); `tambahBarangBaru`: id opsional auto `MNL-…` + tolak nama duplikat persis 409; respons API tak berubah (frontend nol sentuh).
- [x] Gerbang scan keluar (2026-09-09, backend lolos + DB bersih) — `POST /:id/kirim` wajib `pindaian` eksak (kurang→409 sebut ID, asing→400, kosong→409) + jejak `dipindai/cara` + riwayat; `VerifikasiKirim.jsx` di halaman Scan (mode kiriman via state; cek DB + cocok line + manual fallback bertanda; lengkap→Tandai aktif + layar link); hub tombol jadi `Pindai & Tandai`, modal kirim lama dicabut. Tanpa gerak stock di verifikasi (Masuk/Keluar manual tetap satu-satunya via scan biasa).
- [~] T3 — DITUTUP 2026-09-20 (isi terserap: foto bukti SELESAI 2026-09-11; dropdown outlet gugur bersama Tab Buat 2026-09-14; sisa tak terdefinisi).
- [x] 2026-09-10 Tab Lacak list langsung — hapus dropdown wajib-pilih → list kartu terbaru-di-atas + filter menyempitkan + klik expand/collapse + `Muat lagi` 20 + tombol Lacak di Daftar lompat tab. File: `DaftarPengirimanPage.jsx`. Build OK. `test_list.md` U21/E21 diperbarui.
- [x] 2026-09-10 Format item Lacak — `Nama | Terkirim: X | Diterima: Y/-` + baris selisih (`N barang hilang/kurang — alasan` / berlebih / keterangan). File: `DaftarPengirimanPage.jsx`. Build OK.
- [x] 2026-09-10 Full input manual (F7) — route `/input` (cari-nama → kandidat → Masuk/Keluar / form baru 10 field), `/scan` redirect (state terbawa), BottomNav "Input", verifikasi kirim manual ketik-ID (`ManualVerifikasiKirim.jsx`); `TambahBarangPage` tanpa ID; file scanner tetap di disk tak di-route. Build + boot OK.
- [x] 2026-09-10 Satuan bebas per transaksi — `parseKonversi` (pola `1 X = N Y` di keterangan) di `prosesTransaksi` + `buatPengiriman` (tolak merah bila faktor tak ada); opsi satuan kembali di `FormBarangSudahAda` + keranjang susulan + preview; `tambahBarangBaru` terima `istilah_data_resep`; API tambah `keterangan/divisi` (frontend nol pecah).
- [x] 2026-09-10 Audit konversi (`audit_konversi.js`) — 425 barang: 224 ganda-satuan, 15 faktor valid, 209 perlu dilengkapi di KETERANGAN.
- [x] 2026-09-10 Konversi 3 jalur — (1) metrik otomatis kg↔gr/liter↔ml tanpa teks (server+form+audit), valid 16→28; (2) `kandidat_konversi.js` → 19 kandidat dari nama untuk approval YA/tidak; (3) tanpa faktor tetap tolak merah (fail-closed). Build OK.
- [x] 2026-09-10 Terapkan 18 kandidat (Olive Oil dilewati) — append ke KETERANGAN; audit valid 28→46, sisa 178.
- [x] 2026-09-10 Konversi terstruktur (konsep user) — kolom `isi_per_gudang` (`1 gu = N satuan`); backfill 34 faktor teks; parse-teks pensiun (metrik kode tetap); form masuk +field isi (opsional); dropdown keluar + preview baca kolom; skrip audit/kandidat/backfill dihapus (CSV arsip disimpan). Pasca-alih: 46 terbuka / 178 terkunci-SO, nol regresi. Build + boot OK.
- [x] 2026-09-10 Hapus `nextIdTransaksi` — yatim sejak `id_transaksi` identity; cabut fungsi + ekspor + import + komentar Sheet basi di `db.js`. Sintaks OK.
- [x] 2026-09-10 Bug Tolak-nular ID kembar — `PutusForm` + `POST keputusan` cocokkan by posisi + cek multiset ID; simulasi 3×`-` lolos (hanya 1 TOLAK); `test_list` U10 +kasus. Build OK.
- Forensik tabrakan ID (2026-09-10): master bersih dua arah (nol id→multi-nama, nol nama→multi-id); 9 ID nyata varian-kapital kosmetik (Kecap Raos ×3, Beras, …); lautan `-` (±400 nama tanpa master); `nama→id-beda` kosong. Fungsional aman (posisi + ID); obat permanen = remap C8.
- [x] T4 Migrasi CSV (dieksekusi 2026-09-10) — gudang: 425 barang + 394 transaksi + 188 saldo-pembuka; pemesanan: 216 pesanan (group outlet+tanggal, mapping status sah) + 153 kiriman; 9 outlet (token baru, 2 lama dibuang); Outlet→Supabase, Sheet dihapus total dari kode. Sisa: 1795 item `-` (remap C8), 209 KETERANGAN, backlog 167 aktif.
- [x] 2026-09-11 Verifikasi ceklis + foto (ganti ketik-ID) — `ManualVerifikasiKirim` tap-kartu by index + 1 foto paket (`utils/fotoBukti.js`: canvas ≤1280 JPEG 0.7, upload browser→Storage REST tanpa dep baru); gerbang `POST /kirim` by posisi (kurang→409 sebut nama, asing→400) + `fotoKirim` opsional, `cara:'ceklis'`; `POST /konfirmasi` + `fotoTerima` opsional; foto tampil di Lacak + terima (form & terkunci). Gagal upload tak blokir Tandai/Kunci. Gate diuji via API (409/400/200) + baris uji dihapus, DB bersih. Build + boot OK.
- [x] 2026-09-11 Cabut alasan umum tolak — tolak per item + keterangan per item cukup (`Ringkasan` sudah memuatnya, outlet/gudang baca itu); `alasanUmum` dicabut dari `POST keputusan` + `PutusForm` + blok tampil `alasanTolak` (kolom DB dibiarkan kosong).
- [x] 2026-09-11 Tab kategori menyamping outlet — `PesanOutletPage.jsx`: accordion diganti bar tab kategori horizontal sticky (1 layar = 1 kategori, klik langsung ganti; badge `N✓` per tab terisi); search tampil flat lintas-kategori + label kategori. Build OK.
- [x] 2026-09-11 Hapus `transaksi.id_kirim` (kolom di-DROP user; tak pernah dibaca) — `catatTransaksi` tanpa param kirim, blok tautan + 1 try/catch kompensasi di `buatPengiriman` dicabut; audit per-kiriman tetap via `pengiriman.items_json`. PRD skema diperbarui.
- [x] 2026-09-11 Total Aset Inventory (moving-average) — kolom `harga_barang` (barang; dibuat manual user di Supabase) + `harga_satuan` (transaksi, jejak nilai); `hitungAvg()` + self-check 3/3 (50rb+65rb→57.500); Masuk wajib `totalBayar`; Keluar dinilai avg; card homepage full-width Rp + `N item belum ada harga`. SQL arsip: `migrasi_harga_barang.sql`. Build OK. Low Stock tak disentuh.
- [x] 2026-09-13 Buka-ulang link surat jalan di hub — kartu DIKIRIM Tab Daftar: Salin Link + Buka (`/terima/<token>`) + Kirim WA (pakai `k.token` yang sudah ada; hilang otomatis pasca-batal). Build OK.
- [x] 2026-09-13 Pagination Riwayat 5/halaman — komponen `Pager` reusable (dipakai Riwayat + Surat Jalan); reset ke hal 1 saat ganti filter/submit/lompat; halaman dijepit otomatis. Build OK.
- [x] 2026-09-13 DROP 2 kolom mati (`alasan_tolak`, `konfirmasi_gudang_pusat`) — kode bersih 6 titik + SQL user terverifikasi hilang di DB; sintaks + self-check OK.
- [x] 2026-09-13 Dropdown kategori tunggal — `InputKategori` Select + `+ Tambah kategori baru…` (input autofocus + kembali ke daftar); nilai lama asing disisipkan; dipakai form baru + edit. Build OK.
- [x] 2026-09-13 Warna kategori aktual — BASAH biru, KERING hijau, CHEMICAL orange, DAIRY putih susu, KEMASAN abu (cabang lama minuman/makanan/bahan-baku mati dicabut). Build OK.
- [x] 2026-09-13 Responsivitas hub (hibrida) — `App.css` dead-code diganti utilitas `.hub-lebar` (100%/720px/960px) + `.hub-grid` (1→2 kolom); `/pesanan` (Daftar+Lacak, filter wrap), `/inventory`, `/history`; outlet tak tersentuh. Build OK, `test_list` U35.
- [x] 2026-09-13 Redesign kartu inventory gudang — ID monospace + status kata (Habis/Menipis; Aman tanpa badge) + angka tabular + sub "dari batas N" + strip ukur stock-vs-batas + meta Divisi/Kemasan(konversi)/keterangan; EditForm/modal tak tersentuh. Build OK.
- [x] 2026-09-13 Edit/hapus kartu inventory — `PUT /api/barang/:id` (whitelist metadata; ID+stock tolak; duplikat 409; satuan wajib ketik-ulang) + `DELETE` cascade miliknya + guard order 409 + `istilah` di GET; tombol Edit biru (form expand) + modal hapus (Nama/Varian/Kategori/Jumlah + Ya/Batalkan). Uji live siklus penuh OK (ID baru `MNL-0426`, guard 409/400, nol sisa). Build OK.
- [x] 2026-09-13 Migrasi fresh gudang (Gate 0 lolos: mapping col→tgl dibuktikan vs DB lama) — wipe transaksi+barang; import 425 barang ID `MNL-0001…` + 583 transaksi (190 SO + 393 harian; SO-1Sep/col33 dilewati ikut T4); duplikat komplementer digabung (Knorr-75/129, Saos-174/175 — T4 dulu ambil separuh hingga SO hilang); 2 tanpa-No ikut; pengayaan dibawa by-nama (isi 34, keterangan 18, divisi 255); total=TOTAL 425/425; verifikasi multiset 583/583 nol selisih. Self-check + API live OK (katalog 425, riwayat 0).
- [x] 2026-09-13 ID barang tanpa tanggal — `buatIdBarang` jadi `MNL-0001…` urut global 4-digit + retry (KRM/PSN tetap tanggal).
- [x] 2026-09-13 Sortir + pagination Surat Jalan — perlu-lapor di atas + arsip di bawah (sort stabil, newest-first per grup); 5/halaman + pager ‹n/m› (lompat dari Riwayat buka halaman yang tepat). Build OK.
- [x] 2026-09-13 Pisah hak surat jalan + thumbnail tiket — hub: cabut `Buka` + `Kirim WA` (tersisa Salin Link + Batalkan + Lacak; verifikasi paket utuh); `GET /api/pesan/:token` sertakan `fotoKirim/fotoTerima` kiriman; tiket outlet tampilkan thumbnail 2 sisi (klik = tab baru). Uji live: link+foto benar per pesanan. Build OK.
- [x] 2026-09-13 Poll ringan `?ringan=1` — poll/visible pakai respons tanpa katalog (frontend pertahankan katalog fetch penuh pertama; mount/submit/lapor tetap penuh); uji live: 44022→1742 bytes, riwayat sama. Build OK.
- [x] 2026-09-13 Polling hemat outlet — Tab Riwayat/Surat refresh tiap 30 dtk (skip saat hidden/saving + fetch saat kembali visible, reuse `muat()`). Build OK.
- [x] 2026-09-13 Redesign tiket Riwayat/Surat Jalan — `TiketPesanan.jsx` reusable (rel status + langkah-berikutnya + alur Pesan·Siapkan·Kirim·Terima + rincian per barang collapse; `BadgeStatus` pindah ke sini); dipakai Tab Riwayat + Surat Jalan; tanpa font/dep baru. Build OK.
- [x] 2026-09-13 Tab Surat Jalan di link pemesanan — `TerimaForm.jsx` reusable (tab + fallback `/terima/:token`); `PesanOutletPage` 3 tab (badge = DIKIRIM perlu lapor; expand inline + arsip terkunci; tombol Riwayat lompat ke tab); `GET /api/pesan/:token` sertakan linkTerima pasca-lapor (arsip); WA link lahir + instruksi tab. Self-check + build OK.
- [x] Foto dua arah paket (terkunci 2026-09-10; SELESAI 2026-09-11) — kolom `pengiriman.foto_kirim` + `foto_terima` ada; bucket public `bukti-kirim` + policy anon-upload terverifikasi (upload 200 + read 200 via REST). Opsi: wajib dua sisi; bucket public `bukti-kirim`; kolom `pengiriman.foto_kirim` + `foto_terima` (SQL user); util kompresi canvas ≤1280px JPEG ±0.7; gudang foto di `ManualVerifikasiKirim` (gagal upload tak blokir Tandai); outlet foto balik di `TerimaBarangPage` (gagal upload tak blokir kunci); tampil di Lacak + halaman terima; upload langsung browser→Storage (anon key ada). SQL: ALTER 2 kolom + insert bucket + 2 policy storage.
- [x] 2026-09-15 Redesign outlet HP (nota gudang: kertas #FFFEF9 + tinta + daun #147A4A; tanpa font/dep baru) — `outlet.css` baru + `PesanOutletPage.jsx`: header nota + 1 blok lengket (tabs+cari+kat) + stepper 44px + nama wrap 2 baris + filter pil 42px. Fix bug: tombol Keluar manggil `keluarGudang`/`navigate` tak-ada → logout outlet benar. Build OK.
- [x] 2026-09-15 Keranjang → tab ikon 🛒 + badge — bar bawah + Modal dicabut total (±90 baris mati dibuang); tabs grid `1fr 1fr 1fr 52px`; tab keranjang konten asli (daftar + nama + Kirim inline, empty-state + Kembali pesan). Build OK, grep sisa bersih.
- [x] 2026-09-15 Kategori geser HP — strip dikeraskan (`nowrap`, `overscroll-behavior-x: contain`, fade kanan) + auto-`scrollIntoView` kategori aktif (hormat reduced-motion). Build OK.
- [x] 2026-09-15 Ganti password → ikon Sandi header + Modal — link bawah (551-576) dicabut; state/logika reuse; CSS `.outlet-modal`. Build OK.
- [x] 2026-09-15 Note parsial surat jalan — root: Note PENUHI dibaca dari item pengiriman (ket outlet, kosong) bukan line pesanan (ket gudang); TOLAK sudah benar. Fix 2 baris `backend/routes/pengiriman.js` (teruskan `lp.keterangan` → `noteGudang`, prefer ket gudang). Verifikasi DB: Susu pesan 50 → kirim 5, ket "Sisa Stock Hanya Sisa 5". Self-check hijau.
- [x] 2026-09-15 Coretan tolak ujung-ke-ujung — `line-through` per-sel diganti overlay `td::after` (`sj-coret`, 2px #8a8a8a, print-safe). Build OK.
- [x] 2026-09-16 Opname sesi massal (SELESAI kode + uji alur live; UAT fisik user) — SQL `migrasi_opname.sql`: `transaksi.keterangan` + `opname_sesi` (SOP-…/HITUNG/REVIEW/SELESAI/BATAL) + `opname_item` (snapshot qty+harga, fisik NULL; CASCADE sesi, tanpa FK barang). Backend `routes/opname.js`: mulai (409 bila ada terbuka; snapshot 419) + hitung (HITUNG/REVIEW, fisik≥0) + review (min 1 terhitung) + kembali (REVIEW→HITUNG, deviasi spek anti-typo-lock) + putus (selisih vs stock SAAT INI = idempoten; Masuk/Keluar avg-netral via avg kini; jejak `Opname #SOP`; →SELESAI) + batal (tanpa tulis). `catatTransaksi` param keterangan best-effort; `GET transaksi` expose keterangan; History chip biru + search. Frontend `OpnamePage.jsx` (daftar/arsip + HITUNG search+paging20 + REVIEW 4 kolom + selisih warna + Putus/Batal/←Hitung) + tombol Opname header `/inventory` + route `/opname` + 8 fn client. Uji live gate-off: mulai→hitung2→review→batal hijau, 0 tulis stock (jejak uji dibersihkan). Self-check + build OK. `test_list` U50/E50.
- [x] 2026-09-15 Inventory mode Tabel Explorer — toggle Kartu/Tabel di header (`LayoutGrid`/`List`, default kartu); tabel rapat: Nama (titik warna kategori + ID mono) | Kategori badge | Stock kanan tabular (merah bila Habis/Menipis) | Harga + kolom Batas (minimum restock, sort; sembunyi ≤480px); header klik-sort; klik baris = expand detail (strip + meta + Edit/Hapus reuse); 20/baris; HP ≤480px Harga + Batas + Kategori sembunyi. `usePagination` dijepit anti-meluber + navigasi deterministik. Build OK.
- [x] 2026-09-15 Chip ID Kirim di History — kolom `transaksi.id_kirim` ditambah lagi (diisi hanya dari `buatPengiriman`; manual = kosong); `catatTransaksi` param `idKirim` + fallback mundur bertahap anti-crash-pra-migrasi; `GET /api/transaksi` expose `idKirim`; kartu History tampil chip mono + search ikut cocokkan. Bonus: amankan `item.id.toString()` crash di `filterTransaksi` (API tak kirim `id`). Self-check + build OK. SQL (`alter table transaksi add column id_kirim text`) oleh user; baris lama = tanpa chip (tak bisa backfill).
- [x] 2026-09-15 Unduh CSV aset — ikon Download di kartu Total Aset homepage; `utils/unduhCsv.js` (delimiter `;` + BOM, escape baku, TOTAL = angka kartu + stamp WIB; tanpa harga = kosong); murni frontend tanpa endpoint. Build OK + uji logika (escape `;`/kutip, total) lolos.
- [x] 2026-09-16 CSV aset per tanggal + modal preview (SELESAI, kode; UAT user) — `asetPerTanggal.js`: `hitungAsetPerTanggal` (dipakai preview + CSV, angka identik) + `asetPerTanggalKeCsv`; posisi akhir H WIB; H pre-migrasi → kosong + TOTAL 0. `UnduhAsetModal.jsx`: tanggal (max hari ini) + preview hanya bermutasi (16 Sep = 62 baris + TOTAL Rp 1.055.259.816, cocok CSV) + Unduh kini/per-tanggal. Ikon Download kartu Aset → modal (baris tanggal inline yang meluber ke kartu Barang Keluar dicabut). Backend expose `dibuatPada/hargaSatuan/idTransaksi`. Live: 415 pasti + 3 perkiraan (stock>0 tanpa harga). Self-check + build OK.
- [x] 2026-09-16 Master vendor + CRUD (SELESAI, kode; UAT user) — skema live: `id identity PK | nama_vendor | nomor | alamat | created_at` (baris uji `Er Coffe` dihapus user; tabel kosong sehat). Temuan pra-bangun: id tanpa identity (probe id null) + `nomor` numeric makan nol depan (`0812…`→`812…`) — SQL susulan: identity + PK + `created_at` default (hijau, id auto 2) + `ALTER nomor TYPE TEXT` (menunggu user). Backend `routes/vendor.js`: GET + POST (nama wajib + mirip 409) + PUT (whitelist + mirip-kecuali-diri) + DELETE (tanpa guard relasi) + mount; semua `wajibGudang`. Frontend: seksi Vendor di homepage (ikut loading/refresh; edit inline di kolom yang sama + Simpan/Batal + tambah grid berlabel; guard id + nama-kosong; `OK`/`Collapse` dicabut) + 4 fn client. Self-check + build OK; live `/api/vendor` 401 (gate hidup). Tanpa relasi barang/transaksi, tanpa search/filter.
- [x] 2026-09-16 Migrasi barang baru `DAFTAR BARANG.csv` (SELESAI + verifikasi live) — skema final user: `id_barang PK | nama_barang | merk (=varian lama) | kategori (=kategori_bahan lama) | satuan | satuan_gudang | isi_per_gudang | total | harga_barang (avg) | keterangan | minimum_stock | dibuat_pada` (tanpa divisi/istilah). Aturan: sub-header dibuang; satuan = wadah hitung (dus/karton/ctn beda; kalimat isi → keterangan); QTY apa adanya kosong→0; duplikat digabung (Gula/Mocaf/Gas; Kapulaga lalu dipecah lagi, lihat entri berikut); satuan_gudang kosong semua + wajib-keterangan bila diisi (validasi PUT/POST/form); isi_per_gudang opsional NULL. Eksekusi via `jalan_migrasi.py` (bulk REST, abort bila tak kosong): live 419 master + 63 saldo-awal Masuk + `__probe__` 0. Skema susulan user: backfill tgl+id + identity + PK/FK/index + restart sequence — terverifikasi live: PK barang (duplikat→409), FK transaksi (yatim→23503), identity auto. Sampel live: Ajinomoto 0/`12500`, Mie Burung Dara 45. Kode: backend remap merk/kategori (kontrak API `varian/kategori` tetap); `kategori.js` +FROZEN/PERLENGKAPAN + warna StockTable; `test_list` U33/E33/U36/E27/U48. Self-check + `node --check` + build OK; boot live `/api/barang` 401 (gate hidup).
- [x] 2026-09-16 Pecah Kapulaga + satuan Gula/Mocaf (SELESAI live) — Kapulaga baris-96 (100gr) ternyata barang beda: MNL-0093 Pack 3000×180.000 (ket `pack`) + MNL-0419 100gr 3×16.875 + saldo id-16→3000 + saldo baru id-65; baris-384 (tanpa qty) gugur. Gula MNL-0072 satuan→`Kg`, Mocaf MNL-0232→`Gr` (stock 0, nol rupiah). Generator: pengecualian Kapulaga-100gr (MNL lama tak geser) + satuan-nyata-menang + alias `g` (sempat bikin Mocaf `G`). TOTAL live 1.055.259.816 → **1.054.770.441** (pas rumus qty×hrg-20/08 + 3 baris blank dihitung manual).
- [x] 2026-09-16 Basmi kedip halaman gudang — `RequireGudang.jsx` stale-while-revalidate (flag ada → render langsung + verifikasi background; 401 → redirect; tanpa flag → langsung login; blank flash tiap navigasi hilang). `DaftarPengirimanPage.jsx` poll hemat: signature id+status (+token/username/punyaPassword/belumBaca), setState hanya bila berubah (poll 20 dtk tanpa data baru = tanpa render ulang). Build OK; UAT: pindah halaman tanpa blank + diam 1 mnt tanpa kedip + order baru ≤20 dtk.
- [x] 2026-09-16 Putus loop `/` ↔ `/gudang-masuk` — akar: tab baru tanpa flag langsung divonis `tutup` → login, padahal cookie lintas-tab valid → efek login memantulkan balik → loop. `RequireGudang.jsx`: tanpa flag → splash `Memeriksa sesi…` + cek server (valid → buka, 401 → login); dengan flag → render langsung + verifikasi background. Sempat pecah build (ekor fungsi ganda) → diperbaiki + build OK. UAT: tab baru + cookie hidup → `/homepage` langsung; cookie mati → login diam.
- [x] 2026-09-16 Keterangan kembali opsional (revisi aturan kemarin) — baris otomatis `Kemasan: 1 Dus = 24 Pcs` sudah menjelaskan kemasan sehingga keterangan-kemasan = double. Cabut guard PUT/POST + 2 cek client + label netral; data 100+ keterangan import utuh; `test_list` U33/E33. Self-check + build OK.
- [x] 2026-09-16 Peringatan ganti satuan — `StockTable.jsx` blok kuning: `500 Gr jadi 500 Pcs — bukan hasil konversi.` (dinamis ikut angka/satuan; ganti kalimat lama "ikut berubah makna"). Build OK.
- [x] 2026-09-18 Preview avg otomatis di form Input (SELESAI, kode; UAT user) — `FormBarangSudahAda.jsx`: baris `Harga kini (avg)` selalu tampil + preview hidup `Avg baru → Rp Y (dari Rp X)` (copy `hitungAvg`, qty eceran; null → `Avg pertama`); Keluar `avg tetap`; angka ribuan `toLocaleString` bulat (`formatRibu` merusak desimal). Backend nol sentuh. Verifikasi: 3/3 kasus + build OK.
- [x] 2026-09-18 Satuan kanonik + backfill (SELESAI) — `kanonikSatuan()` di `lib/konversi.js`; POST/PUT/GET + `data.js`/`pesan.js`/`opname.js` + 4 file frontend default `pcs`; guard edit bandingkan norm. Backfill live via REST: 419 baris (416 patch, NIHIL liar). `node --check` + uji alias + build OK.
- [x] 2026-09-18 Hapus pengaman `harga_barang` + bersih file sekali-pakai — 2 cabang `Kolom belum ada...` di `routes/barang.js` dibuang (DB sekali-beres; error mentah bila kolom hilang). Hapus: `migrasi_barang_baru/gudang/opname.sql`, `jalan_migrasi.py`, `skrip_impor_barang.py` (+pyc). Sisa: `migrasi_harga_barang.sql` (arsip). Sintaks OK.
- [x] 2026-09-18 Forensik lonceng mati (SELESAI) — akar ganda di tabel `notifikasi`: (1) `id` tanpa auto-nomor → tulis gagal sunyi (`lonceng gagal...` di log, 0 baris); (2) `dibaca` tanpa default → baris NULL tak terhitung badge + tak bisa ditandai-baca. Fix: SQL user (`id` identity) + REST `dibaca=false`. Frontend tak bersalah. Sisa SQL user: default `dibaca=false` + `dibuat_pada=now()`.
- [x] 2026-09-18 Server ganda (SELESAI) — 2 proses `node server.js` (background + terminal, 1 pegang :3000): restart terminal tak mematikan yang asli → sesi utuh → "langsung homepage". Fix: bunuh semua → 1 server segar (sesi hangus = perilaku restart benar). SOP: 1 server saja.
- [x] 2026-09-18 Pecah tab Selesai (SELESAI, kode; UAT user) — hub Daftar: `Diterima` (DITERIMA + SEBAGIAN, badge hijau/kuning) + `Ditolak`; fallback `TAB_ALUR[4]` → by-id. Build OK.
- [x] 2026-09-18 Sesi outlet per-link (SELESAI, kode; UAT U43-45 ulang oleh user) — cookie `sesi_outlet_<token>`; buka/login link B tak membunuh A; cabut `delete` saat mismatch. File: `lib/auth.js`, `routes/pesan.js`. Sintaks OK; server restart (sesi hangus).
- [x] 2026-09-18 Rekonsiliasi aset (SELESAI analisis) — dashboard Rp 1.054.770.441 = Σ(QTY×H20) file CSV persis (TOTAL BIAYA = pos lain 1.025M). Item baru MNL-0420 (Susu UHT 24×8333,33 = 200rb, sempat Ultra 20×5000) lalu dihapus bersih → kembali baseline. Sheet user 1.054.644.441 = range P3:P425 minus baris 140 (Mie `45 PCS/ CTN` #VALUE!, 144rb) & 264 (0) → 1.054.626.441 + misteri +18.000 (terbuka).
- [x] 2026-09-18 Eceran-saja penuh (SELESAI, kode; UAT MNL-0420 oleh user) — akar kasus dus→karton = snapshot `dipilih` basi (API benar 20). Dropdown satuan dicabut (`FormBarangSudahAda.jsx` −30 baris); label `Jumlah ({eceran})`; tombol mati bila ≤0 (tutup celah trx-0). Backend eceran-saja sebelumnya. Build OK.
- [x] 2026-09-18 Filter outlet Batal→Ditolak (SELESAI, kode; UAT user) — Riwayat outlet: Aktif/Diterima/**Ditolak**/Semua (= DITOLAK saja); sejajar tab hub. File: `PesanOutletPage.jsx` 2 baris. Build OK.
- [x] 2026-09-18 Ledger kartu inventory (SELESAI, kode; UAT user) — blok Harga (avg/satuan) + Total (stock×avg, daun, tabular) + ajakan isi bila tanpa harga; garis-ganda dicabut + ukuran dirampingkan atas permintaan (2 revisi). File: `StockTable.jsx`. Build OK. Mode tabel tak tersentuh.
- [x] 2026-09-18 Hero Total Aset HP (SELESAI, kode; UAT user) — Aset full-width urutan pertama + `fs-4` + wrap-anywhere (ekor 57.541 tak luber lagi); prop `ukuran` di `SummaryCard`; varian rata-rata kanan dicoba lalu DIBATALKAN user (revert bersih). Build OK.
- [x] 2026-09-18 Cabut Status unduh CSV (SELESAI, kode; UAT user) — kolom Status hilang dari preview + CSV per-tanggal (8 kolom); komputasi `status`/`campur` dicabut total. File: `UnduhAsetModal.jsx`, `asetPerTanggal.js`. Build OK.
- [x] 2026-09-18 Navbar warna per-tab (SELESAI, kode; UAT user) — aksen Home biru/Inventory daun/Pesanan ungu/History merah/Input hijau + pil aktif; `Nav.Link`→`NavLink` murni (perantara menelan class) + cabut bold (menggeser ukuran). File: `BottomNav.jsx`, `App.css`. Build OK.
- [x] 2026-09-18 DB baru + bucket (SELESAI, user) — bucket `bukti-kirim` public + 2 policy (baca publik, anon upload); `foto_surat_jalan` yatim (saran hapus). Kode 1 baris (`fotoBukti.js`) tak berubah. RLS tabel (anon DENY) + HTTPS/`Secure` + kunci CORS + prune sesi = rencana eksekusi ~30 mnt (belum diminta).
- [x] 2026-09-18 Vendor di transaksi Masuk (SELESAI, kode; UAT user) — dropdown vendor opsional di form Masuk (`FormBarangSudahAda` + `FormBarangBaru`, self-fetch master) + `vendor` diteruskan ke `POST /api/prosesTransaksi` + `POST /api/tambahBarangBaru` → snapshot `transaksi.keterangan = "Vendor: X"` (Keluar/opname tanpa vendor); History chip hijau + search nama langsung ketemu. Tanpa migrasi DB (baris lama tanpa chip); `id_vendor` FK menyusul (lihat bawah). File: `backend/routes/barang.js`, `FormBarangSudahAda.jsx`, `FormBarangBaru.jsx`, `InputManualPage.jsx`, `TransaksiBarangPage.jsx`, `TransaksiTable.jsx`. Sintaks + build OK.
- [x] 2026-09-20 Relasi `transaksi.id_vendor` → `vendor(id)` (SELESAI kode + SQL hijau user) — FK `ON DELETE SET NULL` + index + backfill by-nama; `catatTransaksi(..., idVendor)` + fallback pra-migrasi; `vendorKeCatat()` (`vendorId` → lookup master, 400 bila tak dikenal; legacy nama best-effort); dropdown kirim ID; `GET /api/transaksi` expose `idVendor`. File: `lib/data.js`, `routes/barang.js`, 2 form + 2 pemanggil. `node --check` + build OK.
- [x] 2026-09-20 Hardening A1 (SELESAI kode; RLS terverifikasi user) — RLS on + no policies di 10 tabel (anon DENY; app service_role; bucket bukti-kirim untouched); cookie `Secure` kondisional HTTPS + `trust proxy` (3 titik Set-Cookie); CORS whitelist (localhost + `*.ngrok-free.dev` + ENV `CORS_ORIGIN`); prune sesi/rate per jam (`.unref`). File: `lib/auth.js`, `server.js`, `routes/gudang.js|pesan.js`. `node --check` + self-check 7/7+3/3 + build OK. Perlu restart (sesi hangus). Sisa: rotasi key/WAF saat prod permanen.
- [x] 2026-09-20 Bersih-bersih order uji (SELESAI live) — hapus outlet central-kitchen + 9 pesanan (7 miliknya + 2 DITERIMA Edhos/Foodtruck) + 3 kiriman DITERIMA 17 Sep (semua uji coba). Sisa: 8 outlet, 2 pesanan DITOLAK arsip, 0 kiriman. Stock TANPA disentuh (sudah final CSV; kembalikan-dipotongan dibatalkan user). Nol ubah kode. Skrip dihapus.
- [x] 2026-09-20 Koreksi salah-baca Kertas Cap Gajah (SELESAI live) — `10.500` = 10.500 pack (TOTAL 252jt cocok), bukan 10,5: MNL-0425 total 10,5→10.500 + Masuk 10.500 @Rp24rb tercatat (audit kini lengkap). Sempat 522 sekali (transien, state dicek bersih lalu retry OK).
- [x] 2026-09-20 Koreksi rename-beda-barang (SELESAI live, 424→427) — Kertas/Monin/Tissue ternyata beda barang: 3 nama + harga dikembalikan (MNL-0099/0146/0244 utuh); CSV masuk sebagai MNL-0425..0427 (Cap Gajah pack Rp24rb stock 10,5; Monin 700ML botol; LIVI pack). Thermal tetap rename (sama). Temuan: `transaksi.jumlah` BIGINT = bilangan bulat saja (10,5 ditolak `22P02`); MNL-0425 tetap 10,5 tanpa baris transaksi atas keputusan user. Skrip dihapus.
- [x] 2026-09-20 Sync master barang dari `DAFTAR BARANG.csv` (SELESAI live, 419→424) — CSV menang: INSERT 5 (MNL-0420..0424: Shitake, Salam, Lengkuas, Baguette Regent, Choco Tulip 1Kg); RENAME 4 (ID utuh: Kertas Cap Gajah, Monin 700ML, Thermal 57x38, Tissue LIVI); UPDATE 26 harga + Garpu→KEMASAN + merk Gula/Mocaf dikosongkan + ket 20x20 ke Box Bento/Kertas Wrap; QTY-ADJUST 3 via Masuk @avg kini (Foil+1, Ciao+3, Clink+1); qty-kosong diabaikan. FLAG: Kertas stock 10500 vs CSV 10,5 (konflik satuan, stock tak disentuh); 8 harga rasio ≥5x ikut CSV (Greenfield, Diamond, Oatside, Paseo, Ketumbar, Tahu ×2, Kacang Atom). Skrip sekali-pakai dihapus.
- [x] 2026-09-20 Impor 71 vendor dari `DAFTAR BARANG.csv` (SELESAI live, tabel sebelumnya kosong) — union `VENDOR EKSISTING` + `NAMA VENDOR BARU` (78 grup case-insensitive − 7 merge = 71); merge: RAHYU→RAHAYU, kang Ramdan→Ramdan, toko senopati→Toko Senopatie, Mandiri dian→MANDIRI (DIAN), TOKO SINGA→SINGA, Sejati→Toko Sejati, Sukanda Jaya→Sukanda; varian kapital ikut suara terbanyak; `online` ikut apa adanya; `NEXT VENDOR` diabaikan (isi nomor/spec/nama orang); nomor/alamat kosong. Skrip sekali-pakai dihapus; verifikasi 71/71 sama persis.
- [x] 2026-09-20 Cabut total `/terima` link-only + surat jalan wajib login (SELESAI kode; UAT user) — route `/terima/:token` + `TerimaBarangPage.jsx` + endpoint uuid (`GET /api/pengiriman/:token/lihat`, `POST .../konfirmasi`) + Salin Link/Kirim WA surat jalan dicabut total; pengganti: `GET/POST /api/pesan/:token/surat/:idKirim[/konfirmasi]` (`wajibOutlet` + cek milik `id_pesan.token_outlet`); `TerimaForm` prop `{tokenOutlet, idKirim}`; `GET /api/pesan/:token` kirim `idKirim` (bukan link); `Tandai Dikirim` tanpa token; form login + teks lupa-password (SOP gudang reset). File: `backend/routes/pesan.js|pengiriman.js`, `backend/lib/data.js`, `client.js`, `TerimaForm.jsx`, `PesanOutletPage.jsx`, `App.jsx`. Build + self-check OK; grep `/terima/|linkTerima|tokenDariLink|lihatPengiriman|konfirmasiTerima|wa.me` nol. Perlu restart server (sesi hangus).

## Hutang / bug diketahui
- Chunk JS > 500 kB warning (pre-existing, abaikan).
- Aturan: user edit manual kadang; eksekusi oleh AI hanya saat diminta eksplisit.
- 2026-09-10: file scanner dilarang dihapus (keputusan user — DICABUT 2026-09-14, kamera/scan dibuang total); TOTAL CSV tidak dipercaya sebagai stock akhir sampai user menjelaskan rumus/sumbernya.
- [x] 2026-09-10 Cabut total_awal + total_pembanding — SO 12 Agu diselamatkan sebagai 188 baris saldo-pembuka di `transaksi` (237 nol dilewati); `total` satu-satunya sumber stock. SQL DROP oleh user.
- [x] 2026-09-20 Revisi U30–U32/E30–E32 + teks F7 pasca-eceran-saja — U22/U30-U32/E30-E32 + F7 diselaraskan (form manual eceran-saja, tolak non-eceran pesan baru; `isi_per_gudang` jalur pesanan).
- [~] Misteri +Rp 18.000 — DITUTUP 2026-09-20: error sisi sheet (`#VALUE!` baris Mie `45 PCS/ CTN`, 144rb) membuat total sheet tak valid; dashboard app = Σ(QTY×HARGA) CSV persis, tak diubah. Perbaikan sheet (qty Mie → `45`): di luar aplikasi.
- SOP server: terminal user saja (AI tak pernah menyalakan server; hanya curl/REST verifikasi + bilang "restart"); 1 proses; tiap restart = semua logout (sesi memori).

## Cara lanjut sesi baru
1. Baca `PROGRESS.MD` + `PRD.MD` + `test_list.md`.
2. Perintah mulai: "eksekusi T2" (atau step kecilnya).
